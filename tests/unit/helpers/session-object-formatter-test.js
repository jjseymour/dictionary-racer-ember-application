import { sessionObjectFormatter } from 'dictionary-racer-ember-application/helpers/session-object-formatter';
import { module, test } from 'qunit';

module('Unit | Helper | session object formatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sessionObjectFormatter([42]);
  assert.ok(result);
});
